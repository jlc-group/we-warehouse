# 🚨 แก้ไขปัญหาโปรแกรมค้าง - URGENT

## สาเหตุ
โปรแกรมค้างเพราะมีข้อมูลใน database ที่ `sku` เป็น `NULL` หรือว่างเปล่า ซึ่งทำให้ TypeScript types ที่เพิ่งอัปเดตไม่ตรงกับข้อมูลจริง

## วิธีแก้ไข (ทำตามลำดับ)

### ขั้นตอนที่ 1: แก้ไขข้อมูลใน Database (สำคัญมาก!)
1. เปิด **Supabase Dashboard** → SQL Editor
2. Copy SQL จากไฟล์ `FIX_NULL_SKU_DATA.txt`
3. Run SQL เพื่อ:
   - ตรวจสอบว่ามีข้อมูล SKU เป็น NULL กี่รายการ
   - แก้ไขโดยสร้าง SKU ใหม่จาก product_name และ ID
4. ตรวจสอบว่าแก้ไขสำเร็จ (ควรได้ 0 items without SKU)

### ขั้นตอนที่ 2: Clear Browser Cache
```bash
# ใน browser
1. กด Cmd + Shift + Delete (Mac) หรือ Ctrl + Shift + Delete (Windows)
2. เลือก "Cached images and files"
3. กด Clear data
```

### ขั้นตอนที่ 3: Clear Vite Cache และ Restart
```bash
# ใน terminal
cd /Users/chanack/Documents/GitHub/we-warehouse
rm -rf node_modules/.vite
npm run dev
```

### ขั้นตอนที่ 4: Hard Refresh Browser
```
Cmd + Shift + R (Mac)
หรือ Ctrl + Shift + R (Windows)
```

## ตรวจสอบว่าแก้ไขสำเร็จ

✅ หน้าเว็บโหลดได้
✅ ไม่มี error ใน console
✅ สามารถดูรายการสินค้าได้
✅ สามารถกดปุ่มต่างๆ ได้

## ถ้ายังมีปัญหา

### เช็ค Browser Console
1. เปิด DevTools (F12)
2. ไปที่ Console tab
3. ถ่ายภาพ error messages ทั้งหมด
4. แชร์มาให้ฉันดู

### เช็ค Network Tab
1. เปิด Network tab
2. Refresh หน้า
3. ดู requests ที่เป็นสีแดง (failed)
4. คลิกดู Response เพื่อดู error details

## การป้องกันในอนาคต

Code ที่ฉันแก้ไขไปแล้วมี **SKU Sanitization** ที่จะ:
- ตรวจสอบว่า SKU เป็น null หรือไม่
- สร้าง temporary SKU จาก ID ถ้า SKU หายไป
- ป้องกันไม่ให้ app crash

แต่ควรแก้ไขข้อมูลใน database ให้ถูกต้องด้วย!

## SQL Scripts ที่เกี่ยวข้อง

1. `FIX_NULL_SKU_DATA.txt` - แก้ไข NULL SKU
2. `CREATE_SYSTEM_EVENTS_TABLE.txt` - สร้าง system_events table
3. `FIX_SYSTEM_EVENTS_RLS.txt` - แก้ไข RLS policies
4. `CHECK_INVENTORY_ITEMS_SCHEMA.txt` - ตรวจสอบ schema

## การอัปเดตที่ทำไปแล้ว

✅ อัปเดต `inventory_items` types ให้ตรงกับ database
✅ เพิ่ม SKU sanitization ใน secureGatewayClient
✅ เพิ่ม columns ที่ขาดหาย: batch_code, lot_number, expiry_date, etc.
✅ แก้ไข `sku` เป็น required field (ตรงกับ database)

---

**หมายเหตุ:** ถ้าทำตามขั้นตอนแล้วยังมีปัญหา กรุณาแชร์:
- Screenshot ของ browser console errors
- Screenshot ของ Network tab (failed requests)
- บอกว่ากำลังทำอะไรตอนเกิดปัญหา
