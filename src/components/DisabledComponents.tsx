import React from 'react';

export const LocationManagement = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Location Management</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const LocationManagementNew = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Location Management (New)</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const LocationTransferModal = ({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) => null;

export const LocationQRModal = ({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) => null;

export const ProductSummaryTable = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Product Summary</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const QRCodeManagement = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">QR Code Management</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const UnitConversionSettings = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Unit Conversion Settings</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const UserManagement = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">User Management</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const QRScanner = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">QR Scanner</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const WarehouseDashboard = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Warehouse Dashboard</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const LocationAddItemModal = ({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) => null;

export const LocationExportModal = ({ open, onOpenChange, locations }: { open: boolean; onOpenChange: (open: boolean) => void; locations: any[] }) => null;

export const DatabaseTester = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Database Tester</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const UserProfile = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">User Profile</h2>
    <p className="text-muted-foreground">This component is temporarily disabled due to database schema updates.</p>
  </div>
);

export const RegisterForm = () => (
  <div className="p-8 text-center">
    <h2 className="text-xl font-semibold mb-4">Register</h2>
    <p className="text-muted-foreground">Registration is temporarily disabled due to database schema updates.</p>
  </div>
);